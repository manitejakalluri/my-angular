import { CaptialDirective } from './captial.directive';

describe('CaptialDirective', () => {
  it('should create an instance', () => {
    const directive = new CaptialDirective();
    expect(directive).toBeTruthy();
  });
});
